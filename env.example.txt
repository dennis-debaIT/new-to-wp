# Diese Datei ist nur noch als Referenz/Fallback gedacht.
#
# Ab jetzt lassen sich ALLE Werte hier bequem über die Weboberfläche
# eintragen: Button "⚙️ Einstellungen" oben rechts -> Popup ausfüllen ->
# "Einstellungen speichern". Das Tool schreibt die Werte dann selbst in
# die .env und übernimmt sie sofort (ohne Neustart, Ausnahme: LOG_FILE).
#
# Diese Datei musst du also normalerweise NICHT mehr von Hand bearbeiten -
# es reicht, das Tool einmal zu starten (auch ganz ohne .env) und alles im
# Browser einzutragen.

# --- WordPress ---
WP_URL=https://deine-domain.de
WP_USER=dein-wp-benutzername
WP_APP_PASSWORD=xxxx xxxx xxxx xxxx xxxx xxxx

# --- KI (Groq) ---
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxx
GROQ_MODEL=llama-3.3-70b-versatile
GROQ_TEMPERATURE=0.5

# --- Bild (Pexels) ---
PEXELS_API_KEY=
PEXELS_ORIENTATION=landscape

# --- Verhalten ---
DEFAULT_POST_STATUS=draft
NEWS_LANG=de
NEWS_COUNTRY=DE
SEARCH_RESULT_LIMIT=15
ARTICLE_TONE=sachlich
ARTICLE_LENGTH=mittel

# --- Duplikate & Logging ---
HISTORY_FILE=posted_history.json
LOG_FILE=app.log

# --- Login-Schutz (optional) ---
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=

# --- Telegram-Benachrichtigung (optional) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- Cronjob-Automatik (cron_post.py) ---
NEWS_QUERIES=Smart Home,Photovoltaik
CRON_MAX_POSTS_PER_RUN=1
CRON_DEFAULT_CATEGORY_NAMES=Automatisch
CRON_DEFAULT_TAG_NAMES=
